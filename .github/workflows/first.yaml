name: print firt pipeline

on:
  push:
    branches:
      - main
      - hot-fix/1.0

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: showing demo to naveen
        run: echo "hello i am naveen"
      - name: mentor by shaik
        run: echo "mentor by shaik"
      - name: print linux command
        run: |
          pwd
          whoami
          date
          du
          free

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: testing the test job
        run: echo "Hi I am test step from test job"
      - name: test step 2
        run: echo "iam second step"

  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: test
    steps:
      - name: deploying
        run: echo "deplying into  the target server/environment"
      - name: status check
        run: echo "checking the deployment"
    

        
    
    

