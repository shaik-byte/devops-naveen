name: Scheduled Workflow

on:
  schedule:
   - cron: '*/1 * * * *'
    
jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Run scheduled task
        run: echo "This workflow runs every day at 9:00 AM UTC"
